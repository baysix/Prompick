# 빌드 컨텍스트에서 뺄 것들.
#
# Docker 는 여기 적힌 것을 뺀 나머지를 전부 데몬에 올린 뒤 빌드를 시작한다. 로컬 빌드 산출물은
# 컨테이너 안에서 어차피 새로 만들므로 올릴 이유가 없다 — 그냥 배포가 느려질 뿐이다.

build/
.gradle/

# 비밀값. 이미지 안으로 들어가면 이미지를 가진 사람이 전부 볼 수 있다.
.env
.env.*

# 로컬 저장소 구현이 파일을 쌓아두는 곳 (소스의 storage 패키지가 아니다)
/storage/

*.log
